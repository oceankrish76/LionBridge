# To install it you need 
Python >= 3.6
pip3 

# Installation process!!
- Make virtual environment using python3 
    python3 -m virtualenv testVenv
- activate virtualenv 
   source testVenv/bin/activate
- install all requirements 
  pip3 install -r requirements.txt 
         or 
  python -m pip install -r requirements.txt
- run the app 
  python app.py


# Thoughts
This file just explains the idea of rest endpoints. However in production environment 
I would use REST Frameworks like Django rest frameworks, Flask rest Framework or other
frameworks. The advantages would be I can 